@{
    // >> Process | View | Classic | Case
    @Case()
}
@using Crimenuts.App.Web.Models
@model ProcessModel

@functions {


}

@helper Case()
{
    @RoomView.Record(
        Shared.Format("{0}{1}:", Localization.Text("Game:Case #"), Model.CaseId), 
        Summary(), 
        RoomView.HistoryClass)
}

@helper Summary()
{
    @Localization.NumPlural("{0} {1}", Model.Case.MurderersOpenNum, "murderer")
}
